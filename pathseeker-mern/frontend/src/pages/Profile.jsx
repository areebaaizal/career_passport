import { useState } from 'react';
import { api } from '../services/api';
export default function Profile(){const u=JSON.parse(localStorage.getItem('user')||'{}');const [form,setForm]=useState({name:u.name||'',education:'',skills:'',interests:''});const [msg,setMsg]=useState('');const save=async e=>{e.preventDefault();try{const {data}=await api.put('/auth/profile',form);localStorage.setItem('user',JSON.stringify(data.user));setMsg('Profile updated successfully.')}catch(e){setMsg(e.response?.data?.message||'Profile update route is not available yet.')}};return <main className="card page-card"><span className="eyebrow">ACCOUNT</span><h1>My Profile</h1><p className="muted">Keep your education, skills and interests updated for better recommendations.</p>{msg&&<div className="alert">{msg}</div>}<form onSubmit={save}><label>Full name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input value={u.email||''} disabled/></label><label>Education<input value={form.education} onChange={e=>setForm({...form,education:e.target.value})} placeholder="e.g. BS Computer Science"/></label><label>Skills<input value={form.skills} onChange={e=>setForm({...form,skills:e.target.value})} placeholder="React, JavaScript, SQL"/></label><label>Interests<input value={form.interests} onChange={e=>setForm({...form,interests:e.target.value})} placeholder="Technology, design, data"/></label><button className="btn">Save Profile</button></form></main>}
