import {useEffect,useState} from 'react';import {api} from '../services/api';;
export default function SuccessStories(){const [items,setItems]=useState([]);useEffect(()=>{api.get('/stories').then(r=>setItems(r.data||[])).catch(()=>{})},[]);return <main className="content-page"><div className="page-hero"><div><span className="eyebrow">SUCCESS STORIES</span><h1>Real paths. Real progress.</h1><p>Learn how others navigated education, skills and career changes.</p></div></div><div className="story-grid">{items.length?items.map(x=><article className="story-card" key={x._id}><span className="tag">{x.domain||'Career journey'}</span><h2>{x.title}</h2><p>{x.story}</p><strong>— {x.name||'PathSeeker member'}</strong></article>):<div className="empty wide">Stories will appear here after admin approval.</div>}</div></main>}
